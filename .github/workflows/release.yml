# 发布工作流：在 windows-latest 上编译 Rust 版本并发布 GitHub Release
#
# 触发方式：
#   1. 推送 v* 标签（如 v4.1）→ 构建并创建同名 Release
#   2. 推送 main / claude/** 分支 → 构建并上传 artifact（构建验证）；
#      若 .github/RELEASE_TAG 文件里声明的版本（如 v4.1）尚未发布，
#      则在当前 commit 上创建该标签与 Release（只创建一次，已存在则跳过，
#      适用于无法直接推标签的受限环境）
#   3. 手动 workflow_dispatch（工作流文件合入默认分支后可用）：
#      - tag 留空 → 仅构建并上传 artifact
#      - tag 填写（如 v4.1）→ 构建并在当前 commit 上创建标签与 Release
#
# Release 说明取自 .github/release-notes/<tag>.md（首行 `# 标题` 作为 Release 标题），
# 找不到对应文件时使用自动生成的说明；SHA256 校验值由工作流追加。
name: Release

on:
  push:
    branches: ["main", "claude/**"]
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "发布用标签（如 v4.1）。留空则只构建，不创建 Release"
        required: false
        default: ""

permissions:
  contents: write

jobs:
  build-release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: 安装 Rust 工具链
        run: rustup toolchain install stable --profile minimal --target x86_64-pc-windows-msvc

      - name: 编译（release）
        working-directory: rust
        run: cargo build --release --target x86_64-pc-windows-msvc --locked

      - name: 打包
        id: package
        shell: pwsh
        run: |
          $tag = '${{ inputs.tag }}'
          if (-not $tag -and '${{ github.ref_type }}' -eq 'tag') {
            $tag = '${{ github.ref_name }}'
          }
          $fromMarker = 'false'
          $marker = '.github/RELEASE_TAG'
          if (-not $tag -and (Test-Path $marker)) {
            $m = (Get-Content $marker -Raw).Trim()
            if ($m) { $tag = $m; $fromMarker = 'true' }
          }
          if (-not $tag) { $tag = 'dev' }
          "from_marker=$fromMarker" | Add-Content $env:GITHUB_OUTPUT

          $dir = 'WSL-Image-Clipboard-Helper'
          New-Item -ItemType Directory -Path "$dir/temp" -Force | Out-Null
          Copy-Item rust/target/x86_64-pc-windows-msvc/release/wsl_clipboard.exe "$dir/"
          Copy-Item README.md "$dir/"
          @'
          hotkey = "!v"
          runtime_mode = "fast"
          paste_format = "plain"
          path_style = "plain"
          '@ | Set-Content -Encoding utf8 "$dir/wsl_clipboard.toml"
          # 占位文件保证空的 temp/ 目录被打进压缩包
          New-Item -ItemType File -Path "$dir/temp/.keep" -Force | Out-Null

          $zip = "WSL-Image-Clipboard-Helper-$tag.zip"
          Compress-Archive -Path $dir -DestinationPath $zip -Force

          $zipHash = (Get-FileHash $zip -Algorithm SHA256).Hash.ToLower()
          $exeHash = (Get-FileHash "$dir/wsl_clipboard.exe" -Algorithm SHA256).Hash.ToLower()
          "tag=$tag" | Add-Content $env:GITHUB_OUTPUT
          "zip=$zip" | Add-Content $env:GITHUB_OUTPUT
          "zip_sha256=$zipHash" | Add-Content $env:GITHUB_OUTPUT
          "exe_sha256=$exeHash" | Add-Content $env:GITHUB_OUTPUT

      - name: 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: WSL-Image-Clipboard-Helper-${{ steps.package.outputs.tag }}
          path: ${{ steps.package.outputs.zip }}

      - name: 创建 GitHub Release
        if: github.ref_type == 'tag' || inputs.tag != '' || steps.package.outputs.from_marker == 'true'
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.package.outputs.tag }}
          ZIP: ${{ steps.package.outputs.zip }}
          ZIP_SHA256: ${{ steps.package.outputs.zip_sha256 }}
          EXE_SHA256: ${{ steps.package.outputs.exe_sha256 }}
          FROM_MARKER: ${{ steps.package.outputs.from_marker }}
        run: |
          # RELEASE_TAG 标记文件路径只负责“首次发布”；已存在的 Release 不再改动，
          # 避免后续分支推送用新构建覆盖旧标签对应的资产
          gh release view $env:TAG 2>$null
          if ($LASTEXITCODE -eq 0 -and $env:FROM_MARKER -eq 'true') {
            Write-Host "Release $($env:TAG) 已存在，RELEASE_TAG 触发路径跳过更新"
            exit 0
          }

          $title = $env:TAG
          $notesPath = ".github/release-notes/$($env:TAG).md"
          if (Test-Path $notesPath) {
            $lines = Get-Content $notesPath
            if ($lines.Count -gt 0 -and $lines[0] -like '# *') {
              $title = $lines[0].Substring(2).Trim()
              $lines = $lines | Select-Object -Skip 1
            }
            $notes = ($lines -join "`n").Trim()
          } else {
            $notes = "Automated release build for $($env:TAG)."
          }

          $checksums = @(
            ''
            '## Checksums'
            ''
            '```text'
            "SHA256  $($env:ZIP)"
            $env:ZIP_SHA256
            ''
            'SHA256  wsl_clipboard.exe'
            $env:EXE_SHA256
            '```'
          ) -join "`n"
          $notes = $notes + "`n" + $checksums
          $notes | Set-Content -Encoding utf8 release-notes.md

          gh release view $env:TAG 2>$null
          if ($LASTEXITCODE -eq 0) {
            gh release upload $env:TAG $env:ZIP --clobber
            gh release edit $env:TAG --title $title --notes-file release-notes.md
          } else {
            gh release create $env:TAG $env:ZIP --title $title --notes-file release-notes.md --target $env:GITHUB_SHA
          }
